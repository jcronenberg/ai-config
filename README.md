# ai-config

Personal configuration for AI coding tools. Currently contains Claude Code skills and CLAUDE.md templates.

**Warning:** Parts of this configuration were AI-generated and not fully human-reviewed. Only run the install script and use these skills inside a secure, sandboxed environment.

## Install

```sh
./install.sh
```

Symlinks everything under `claude/` into `~/.claude/`.
